{
    "schemaVersion": 2,
    "backend": {
        "version": "1.1.0",
        "downloadUrl": "https://github.com/orgs/Solfins-dev/packages/container/package/3dx-gateway",
        "changelog": "First-run license wizard at /onboarding -- drag/drop or paste your .lic file, signature is validated server-side, no shell needed. Domain allow-list field on the .lic schema (licensedDomains): combine with named users as a UNION so anyone at @company.com can log in up to the seat cap, no .lic re-issue per new hire. Email/OTP onboarding path is gated behind a Licensing__RequestEnabled flag (default false) until the M365 SMTP path goes live; backend returns a friendly 503 + the wizard hides the request CTA when off. Windows port of the Apply Update host helper -- Scheduled Task on TCP 5171 with bearer-token auth, gateway container reaches it via host.docker.internal. Reload() NRE fix on the trial-mode branch.",
        "sha256": null
    },
    "cadBridge": {
        "version": "1.8.27",
        "downloadUrl": "https://github.com/Solfins-dev/3dx-gateway-updates/releases/download/cadbridge-v1.8.27/CadBridge-Setup.zip",
        "changelog": "ADR-015 Phase 2 release. (1) SHA256 verify on Apply Update: agent now downloads from /api/downloads/CadBridge-Setup.zip?raw=true and hashes the bytes locally before letting Setup.bat touch anything; mismatch refuses to install. Legacy v1 manifest without sha256 degrades to warning-log + proceed (no regression for stale gateways). (2) De-elevate launched agent: Setup.ps1 now spawns the new agent at medium IL via a one-shot Scheduled Task (RunLevel=Limited), so the post-install tray runs at the same IL as the reboot-Startup-shortcut path -- ends the Task Manager 'Administrator' inconsistency.",
        "sha256": "9d356223531297b23e7c2c27a3eabe2740cd6c9725893e93678a54d1b3f622f7"
    }
}
