{
    "schemaVersion": 2,
    "backend": {
        "version": "1.1.6",
        "downloadUrl": "https://github.com/orgs/Solfins-dev/packages/container/package/3dx-gateway",
        "changelog": "Generate UI counts (STEP/3DPDF/THUMB) now report the true on-disk state on re-runs instead of undercounting by sub-component files written under baseName fallback. Three-layer fix: frontend collectItemInfo synthesises {name}.SLDPRT/SLDASM fallback keys for itemInfoMap so the agent's platform-key lookup hits when the _cadFilename hint is null; SW post-Generate refresh now passes expectedPdf3D + sets genStatus.pdf3d (V5 path already did this -- SW path was the inconsistency); agent-side cache-hit branches across STEP / Thumb / PDF / DXF / 3D PDF / co-save STEP / V5 STEP now add the on-disk filename to result.GeneratedFiles[*]. result.GeneratedFiles[*] semantics shift from 'freshly written this run' to 'present on disk after this run' (numeric result.Generated counter unchanged). Requires CadBridge 1.8.36 -- bumps land together. Also tightens the Settings -> Telemetry card: disclosure paragraph moves into the toggle's hover tooltip; last-sent + install-id collapse to a single row.",
        "sha256": null
    },
    "cadBridge": {
        "version": "1.8.43",
        "downloadUrl": "https://github.com/Solfins-dev/3dx-gateway-updates/releases/download/cadbridge-v1.8.43/CadBridge-Setup.zip",
        "changelog": "Architectural simplification: EngItem-driven OpenDoc7 is now the sole primary doc-resolution strategy in GenerateOutputsOnSta -- runs for every BOM item instead of being a fallback. SW handles already-open docs idempotently and serves the latest rep automatically because EngItem ids are stable cross-rep (only 3DShape ids rep-version). Eliminates the rep-drift problem class that motivated the prior three-stage lookup. swDocByPlmid index removed entirely (empirically dead on rep-drift tenants -- 23/23 items across PIN-000081 + PIN-000084 resolved via EngItemId, zero strict-PLMID hits). swDocByFilename retained as defensive fallback. ~3s wall-clock overhead per 12-item BOM vs. prior near-zero lookup; acceptable for single-strategy correctness.",
        "sha256": "cffe9eb2a1586238209db7e76f82dd22813a2378330fd65649667f57de6f71ce"
    }
}
