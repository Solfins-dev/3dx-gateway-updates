{
    "schemaVersion": 2,
    "backend": {
        "version": "1.1.0",
        "downloadUrl": "https://github.com/orgs/Solfins-dev/packages/container/package/3dx-gateway",
        "changelog": "First-run license wizard at /onboarding -- drag/drop or paste your .lic file, signature is validated server-side, no shell needed. Domain allow-list field on the .lic schema (licensedDomains): combine with named users as a UNION so anyone at @company.com can log in up to the seat cap, no .lic re-issue per new hire. Email/OTP onboarding path is gated behind a Licensing__RequestEnabled flag (default false) until the M365 SMTP path goes live; backend returns a friendly 503 + the wizard hides the request CTA when off. Windows port of the Apply Update host helper -- Scheduled Task on TCP 5171 with bearer-token auth, gateway container reaches it via host.docker.internal. Reload() NRE fix on the trial-mode branch.",
        "sha256": null
    },
    "cadBridge": {
        "version": "1.8.35",
        "downloadUrl": "https://github.com/Solfins-dev/3dx-gateway-updates/releases/download/cadbridge-v1.8.35/CadBridge-Setup.zip",
        "changelog": "PLMID identity chain end-to-end -- /sw-preflight + /list-open-documents now enrich open docs with plmId (IModelDocExtension.GetPLMID) and SummaryInfo title; frontend matches against the EngItem/3DShape pair via buildOursPidSet, with a trust-the-user step-4 fallback for cases where the chain breaks. Auto-open via /open-from-3dx fires when preflight finds no matching root open (replaces the lost 'Open in SW' affordance after Phase 2c-3 strict-mode gate). Stall threshold 3min -> 10min for slow internet / 3DX cold cache. STEP + 3D PDF now co-save during the post-loop standalone phase -- saves one SW open per component on dual-format runs (~50s for a 10-component BOM on cold cache). New diagnostic GET /api/cadbridge/sw-plm-props dumps every custom property + $PLMPRP probe pattern on the active doc.",
        "sha256": "6592b31bc02b01b13fe2ec4a1992c603ce6eb5d4dc3609ba21e429ea9a5be151"
    }
}
